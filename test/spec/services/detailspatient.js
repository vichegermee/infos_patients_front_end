'use strict';

describe('Service: detailsPatient', function () {

  // load the service's module
  beforeEach(module('infosPsIhmApp'));

  // instantiate service
  var detailsPatient;
  beforeEach(inject(function (_detailsPatient_) {
    detailsPatient = _detailsPatient_;
  }));

//  it('should do something', function () {
//    expect(!!detailsPatient).toBe(true);
//  });

});
